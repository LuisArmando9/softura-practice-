import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'softura_practice',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
